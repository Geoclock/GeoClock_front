import React from "react";
import {Dimensions} from "react-native";

const wWidth = Dimensions.get('window').width;
const wHeight = Dimensions.get('window').height;

export default wHeight
export default wWidth
